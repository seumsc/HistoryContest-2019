import * as validator from "validator"
import { IsAlphanumeric } from "class-validator";
export function isUsernameValid(username:string):boolean {
    return true
}

export function isPasswordValid(passward:string):boolean {
    return true
}