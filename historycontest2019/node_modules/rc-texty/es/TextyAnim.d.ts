import * as React from 'react';
import { ITextyProps } from './TextyProps';
export default class TextyAnim extends React.Component<ITextyProps, {}> {
    static defaultProps: {
        type: string;
        mode: string;
        prefixCls: string;
        component: string;
        delay: number;
        interval: number;
        appear: boolean;
    };
    tweenGrooup: {
        keysToEnter: string[];
        keysToLeave: string[];
    };
    getChildrenToRender: (str: string) => React.ReactNode[];
    getEnterOrLeave: (e: any, genre: any, length: any) => any;
    render(): JSX.Element;
}
